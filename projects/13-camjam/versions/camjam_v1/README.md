# CamJam v1 (frozen archive)

This directory is a **read-only full mirror** of the project as it existed before CamJam v2.

- Do not develop new features here.
- To run the original CLI: `sudo ./run.sh` from this directory (uses bundled `venv/`).
- For current work, use `../../camjam_v2/` and the root `run.sh`.

See `MANIFEST.txt` for file checksums at archive time.